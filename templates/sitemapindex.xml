<?xml version="1.0" encoding="UTF-8"?>
<sitemapindex xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
    {% for timestamp in timestamps %}
        <sitemap><loc>{{ url_for('sitemap', start=timestamp, size=size, _external=True) }}</loc></sitemap>
    {% endfor %}
</sitemapindex> 
